/* FOR IN  

const datos = {
    nombre:'pepe',
    apellido: 'suarez',
    dni: '54543565',
    }

let resultado = ''

for(let propiedad in datos){
    resultado = resultado + '\n' + propiedad + ': ' + datos[propiedad]
    console.log(propiedad)
}

// Propiedad seria nombre, apellido y dni.

console.log(resultado) */